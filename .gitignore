# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
env/
venv/
*.env
*.egg-info/
build/
dist/

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo

# OS files
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/
.DS_Store

# Test tooling. package.json is committed (it declares the jsdom dev
# dependency); the lock file is not, because nothing here pins a build.
node_modules/
package-lock.json

# Pipeline scratch. The generated files under candidate_profiles_site/data/
# ARE committed - the site serves straight from the repository with no build
# step - but their temp files are not.
*.csv.tmp
candidate_profiles_site/data/*.tmp
_inline/
